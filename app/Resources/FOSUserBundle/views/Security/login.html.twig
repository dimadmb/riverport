{% extends "FOSUserBundle::layout.html.twig" %}

{% block fos_user_content %}

	{% trans_default_domain 'FOSUserBundle' %}

	{% if error %}
		<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
	{% endif %}

	<form action="{{ path("fos_user_security_check") }}" method="post">
	  <div class="form-group">
		<input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />

		<label for="username">Логин или Email </label>
		<input class="ui-form-control" type="text" id="username" name="_username" value="{{ last_username }}" required="required" />
		
		</div>
		<div class="ui-form-group">
		

		<label for="password">{{ 'security.login.password'|trans }}</label>
		<input class="ui-form-control" type="password" id="password" name="_password" required="required" />
		</div>
		<div class="ui-form-group">

		<label for="remember_me"><input  type="checkbox" id="remember_me" name="_remember_me" value="on" /> {{ 'security.login.remember_me'|trans }}</label>
	  </div>
		<input class="ui-btn ui-btn_default" type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />
		
		<a style="margin-left:10px;" href="{{path('fos_user_registration_register')}}" class="ui-btn ui-btn_default pull-right">Регистрация нового пользователя</a>
		<a href="{{path('fos_user_resetting_request')}}" class="ui-btn ui-btn_default pull-right">Забыл пароль</a>
		
	</form>
	

	
	
{% endblock fos_user_content %}